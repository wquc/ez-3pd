#ifndef DEFINE_H
#define DEFINE_H

#include <string>
#include <vector>
#include <cmath>

using REAL    = double;
using INT     = unsigned int;
using STR     = std::string;
using REALVEC = std::vector<REAL>;

const REAL COEFF1 = 1/sqrt(3);
const REAL COEFF2 = 2/sqrt(3);
const REAL MATHPI = 3.1415926;

#endif